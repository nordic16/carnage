<div class="md:container px-auto px-4 text-center">
  <%= form_with model: @workout do |form| %>
    <%= form.text_field :title, class: "text-3xl text-center font-bold text-slate-100 bg-slate-900 rounded-full"%><br><br><br>
    <%= form.submit class: "text-slate-100 text-4xl font-bold px-4 py-2 bg-red-500 rounded-full" %>
  <% end %>
</div>
