<div class="md:container px-auto px-4">
  <p class="text-slate-100 text-4xl font-extrabold text-center"><%= @workout.title %></p>
  <%= button_to "Delete", workout_path(@workout), method: :delete  %>
</div>
